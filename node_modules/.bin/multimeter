#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../screeps-multimeter/bin/multimeter" "$@"
  ret=$?
else 
  node  "$basedir/../screeps-multimeter/bin/multimeter" "$@"
  ret=$?
fi
exit $ret
